#include <vector>
#include "numbers.cpp"

template <typename Num>
class Grid{
    // using Ptr = LL_Node<Node<Num>>;
    // using Frac = Fraction<long long>;
    // using Vec = std::vector<*Ptr>;
    // std::vector<std::vector<Vec>> grid;
    // Frac dx, dy;
    // Frac x0, x1, y0, y1;


    // Grid(unsigned NX, unsigned NY, Frac const& x0, Frac const& y0, Frac const& x1, Frac const& y1):
    //     grid{NX, std::vector<*Ptr>(NY)}, dy{y1-y0}, dx{x1-x0}, x0{x0}, x1{x1}, y0{y0}, y1{y1}{}

    // void rasterize_edge(Ptr* P){


    // }
};