stages:
  - build
  - test

create_file:
  image: alpine
  stage: build
  script:
    - echo "Building ..."
    - mkdir build
    - touch build/somefile12.txt
  artifacts:
    paths:
      - build/

test_file:
  image: alpine
  stage: test
  script:
    - test -f build/somefile12.txt