stages:
  - build
  - test

create_file:
  image: alpine
  stage: build
  script:
    - echo "Building ..."
    - mkdir build
    - touch build/somefile.txt

test_file:
  image: alpine
  stage: build
  script:
    - test -f build/somefile.txt